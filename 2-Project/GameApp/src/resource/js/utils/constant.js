const SET_TIMER = 10;
const SET_COUNT = 10;

const ELEMENT_NAMESPACE = {
  TIMER: '.game-timer span',
  FIELD: '.game-field',
  POPUP: '.game-popup',
  GAME_MASSAGE: '.game-massage',
  PLAY_BUTTON: '.play',
  REPLAY_BUTTON: '.replay',
  COUNT: '.game-count .count',
};

export { SET_TIMER, SET_COUNT, ELEMENT_NAMESPACE };
